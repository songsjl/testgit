{
	"version": "2.0.0",
	
    "options": {
		//工作目录
        "cwd":"${workspaceFolder}/build"
	},

	"tasks": [
		{
			"type": "shell",
            "label":"make",
            "group":{
                "kind": "build",
                "isDefault": true
            },
            //提前执行的命令,-G "MinGW Makefiles"生成的项目可以用该命令
            "command":"make -j3",
            "args": []
		}
	]
}
